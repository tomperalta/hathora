import React from "react"

// Libraries
import styled from "styled-components"

const StyledMapLocation = styled.div``

const MapLocation = (props) => {
	/**
	 * PROPS
	 */
	const { region } = props

	return <StyledMapLocation></StyledMapLocation>
}

export default MapLocation
